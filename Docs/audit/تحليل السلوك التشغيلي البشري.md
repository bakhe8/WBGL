بناءً على قراءة الكود الحالي، لدي تصحيح مهم لأحد النتائج في التحليل، ثم سأقدم التقرير الكامل.

تحليل السلوك التشغيلي البشري
تصحيح حرج: نمط-5 (normalizeAmount)
التحليل السابق أشار إلى أن normalizeAmount("500,000.00 SR") يُعيد 500. الكود الفعلي:


<?php$clean = str_replace([',', ' ', 'SAR', 'ريال'], '', $value);// "500,000.00 SR" → "500000.00SR"// is_numeric("500000.00SR") → false → returns null
الكود يُزيل SAR لكن لا يُزيل SR → النتيجة null وليس 500. هذا يعني أن الحقل يبقى فارغاً بدلاً من خاطئ — وهو تصرف مختلف لكنه أيضاً صامت وخطير.

تقرير: تحليل السلوك التشغيلي البشري الواقعي
نمط-1: موظف يستورد نفس ملف Excel مرتين
السيناريو: مستخدم يضغط "استيراد" مرتين بالخطأ أو يُعيد تحميل الصفحة.

ردة فعل النظام:

الاستيراد الأول ينجح.
الاستيراد الثاني يصطدم بـ UNIQUE constraint على guarantee_number.
الخلل الحرج: السطر 195 من ImportService.php:

<?phpstrpos($e->getMessage(), 'UNIQUE constraint')
هذا نص SQLite. رسالة PostgreSQL هي duplicate key value violates unique constraint — لا تطابق أبداً → يُعاد رمي الاستثناء → HTTP 500 بدون رسالة واضحة.
ما يراه المستخدم: خطأ عام بدون توضيح.

الأثر: المستخدم يعتقد أن الاستيراد الأول فشل أيضاً ويحاول مرة ثالثة.

سلامة البيانات: محفوظة (لا تكرار فعلي). لكن حدث duplicate_import لا يُسجَّل لأن الشرط المكسور يمنع الوصول إليه.

🔴 خطورة: عالية

نمط-2: اسم مورد مكتوب بأشكال متعددة
السيناريو:


"شركة النور للمقاولات" / "شركة النور" / "النور للمقاولات والتشييد"
ردة فعل النظام:

ArabicNormalizer يُطبِّع الثلاثة لكن يستبدل ة بـه → "شركه النور للمقاولات".
FuzzySignalFeeder يحسب levenshtein() — لكن PHP's levenshtein() تعمل على bytes لا حروف Unicode → الأحرف العربية (3 bytes كل منها) تُحسَب بشكل خاطئ.
الاسم الثالث الطويل يحصل على درجة تشابه منخفضة رغم أنه نفس المورد.
الأثر طويل المدى:

learning_confirmations تتراكم لكن لا تُحسِّن threshold الـfuzzy.
mb_levenshtein.php موجود في المشروع لكن غير مستخدم في FuzzySignalFeeder.
🟠 خطورة: متوسطة-عالية

نمط-3: مشرف يستخدم Break Glass بشكل روتيني
السيناريو: المشرف يكتشف أن "اعادة فتح" (9 أحرف) كافٍ للتجاوز.

ردة فعل النظام بعد قراءة BreakGlassService.php:


<?php$requireTicket = (bool)$settings->get('BREAK_GLASS_REQUIRE_TICKET', true);if ($requireTicket && $ticketRef === '') {    throw new RuntimeException('رقم التذكرة/الحادث مطلوب');}
إذا كان BREAK_GLASS_REQUIRE_TICKET = true (الافتراضي) → يُطلب رقم تذكرة → حاجز إضافي.
لكن إذا عطَّل مدير النظام BREAK_GLASS_REQUIRE_TICKET → الحد الوحيد هو 8 أحرف.
لا حد كمي يومي على استخدام Break Glass في أي حالة.
الأثر: تضخُّم سجل Break Glass بآلاف أحداث روتينية يُخفي الاستخدامات الاستثنائية الحقيقية.

🟠 خطورة: متوسطة (مع REQUIRE_TICKET=true) | 🔴 عالية (بدونه)

نمط-4: فريق يعالج نفس الدفعة في وقت واحد
السيناريو: 3 موظفين يشغِّلون "مطابقة تلقائية" في وقت واحد.

ردة فعل النظام:

كل موظف يستدعي SmartProcessingService::processNewGuarantees().
استعلام: WHERE d.id IS NULL — بدون SELECT FOR UPDATE.
موظفان يجلبان نفس الضمان → كلاهما يُنشئ قرار → UNIQUE constraint على guarantee_decisions.guarantee_id.
الاستثناء غير مُمسوك في createAutoDecision() → يصل للمستوى الأعلى.
سلامة البيانات: يُحفَظ قرار واحد فقط.

سجل التدقيق: قد يُسجَّل حدثان لنفس الضمان.

🔴 خطورة: عالية

نمط-5: استيراد بيانات بتنسيق مختلف
السيناريو: "500,000.00 SR" كمبلغ.

التصحيح (بعد قراءة الكود الفعلي):


<?php$clean = str_replace([',', ' ', 'SAR', 'ريال'], '', "500,000.00 SR");// → "500000.00SR"// is_numeric("500000.00SR") → false → returns null
ما يحدث فعلاً: المبلغ يُخزَّن كـnull لا 500. الضمان يُنشأ بمبلغ فارغ بصمت.

سيناريو آخر: "500.000" (فاصل أوروبي) → floatval("500.000") = 500 → يُخزَّن 500 بدلاً من 500,000.

لا validation يمنع مبالغ صغيرة جداً أو null.

🔴 خطورة: عالية — أخطاء مالية صامتة

نمط-6: مدير يضغط "التالي" بسرعة على ضمانات متعددة
السيناريو: ضغطات متتالية سريعة على نفس الضمان.

ردة فعل النظام (من workflow-advance.php):


<?php// 6. Execute Transition$decisionRepo->createOrUpdate($decision);  // DB update// ثم بدون transaction:TimelineRecorder::recordWorkflowEvent(...); // Timeline recording
الخلل المؤكَّد: لا TransactionBoundary::run() يجمع تحديث DB مع تسجيل التاريخ.

مقارنةً بـextend.php الذي يستخدم TransactionBoundary::run() بشكل صحيح:


<?php$mutation = \App\Support\TransactionBoundary::run($db, function () use (...) { ... });
→ workflow-advance.php هو الاستثناء المكسور، extend.php هو النموذج الصحيح.

الأثر: ضمان انتقل في DB لكن بدون حدث تاريخي مطابق — أو العكس.

🔴 خطورة: عالية

نمط-7: حذف ضمان ثم إعادة استيراده بنفس الرقم
السيناريو: حذف ضمان منتهٍ ثم استيراد جديد بنفس رقمه.

ردة فعل النظام:

إذا استُدعي HistoryArchiveService → التاريخ يُحفَظ في archived_* tables.
إذا حُذف مباشرة من الواجهة بدون HistoryArchiveService → ON DELETE CASCADE يُفعَّل → التاريخ الكامل يُحذَف بصمت.
الاستيراد الجديد ينجح بتاريخ نظيف.
الخطر القانوني: لا دليل على الضمان السابق في أي تدقيق.

🔴 خطورة: حرجة

نمط-8: حفظ متكرر لبيانات جزئية
السيناريو: 4 حفظات متتالية أثناء إدخال البيانات تدريجياً.

ردة فعل النظام:

كل حفظ → recordManualEditEvent() → حدث manual_edit في التاريخ.
بعد 4 حفظات: 4 أحداث "تعديل يدوي" قبل أي عملية حقيقية.
ConfidenceCalculatorV2 يعتبر الحقول المتغيرة كـ"تعديلات متعددة" → يُشوِّش نموذج التعلم.
سجل التدقيق: صحيح تقنياً لكن مُضخَّم ومُضلِّل.

🟡 خطورة: منخفضة-متوسطة

نمط-9: نسيان تحديد "بيانات اختبار"
السيناريو: استيراد ملف اختبار بدون تحديد is_test_data = true.

ردة فعل النظام:

البيانات تُستورد كـ"حقيقية".
تظهر في الإحصاءات والتقارير.
SmartProcessingService يُطابقها مع موردين حقيقيين.
learning_confirmations تتلوَّث ببيانات وهمية كـ"تأكيدات حقيقية".
لا تحذير أو prompt يسأل "هل هذه بيانات اختبار؟"
التراجع: يتطلب تعديل is_test_data في DB مباشرة أو حذف يدوي لكل ضمان.

🟠 خطورة: متوسطة-عالية

نمط-10: التراجع عن تمديد بعد توقيع الخطاب
السيناريو: تمديد، صدر الخطاب، تبيَّن خطأ في التاريخ.

ردة فعل النظام:

UndoRequestService::submit() يُنشئ طلب تراجع.
بعد الموافقة: بيانات الضمان تُعاد للحالة الصحيحة.
لكن letter_snapshot HTML المُخزَّن في guarantee_history لا يمكن تعديله — يبقى بالتاريخ الخاطئ.
ما هو ناقص: لا خطاب تصحيح يُنشأ تلقائياً عند تنفيذ التراجع.

السجل القانوني: يُظهر خطأً وتصحيحه — صحيح من منظور المحاسبة لكن مُثير للتساؤلات.

🟠 خطورة: متوسطة

نمط-11: البحث بعربية تحتوي تاء مربوطة
السيناريو: بحث عن "شركة النور" بينما المُخزَّن "شركه النور".

ردة فعل النظام:


<?php// NavigationService::buildFilterConditions()$searchSql = " AND (g.raw_data::text LIKE :search_any ...)";// :search_any = '%شركة النور%'// raw_data يحتوي: "شركه النور" (بعد التطبيع)
ArabicNormalizer يُطبِّع عند الاستيراد لكن واجهة البحث لا تُطبِّع نص البحث.

ما يراه المستخدم: "لا توجد نتائج" بينما الضمان موجود.

🔴 خطورة: عالية — تأثير مباشر على تجربة المستخدم اليومية

نمط-12: ضغط "رجوع" في المتصفح بعد تقديم طلب workflow
السيناريو: مستخدم يضغط رجوع ثم يُعيد إرسال النموذج.

ردة فعل النظام:

الطلب الأول نجح: workflow_step = 'audited'.
الطلب الثاني: يُحاول نفس الانتقال → old_step = 'audited', new_step = 'audited' → لا استثناء من DB trigger.
لكن: WorkflowService::getNextStage('audited') يُعيد 'analyzed' — الطلب الثاني قد يُقدِّم للمرحلة التالية بشكل غير مقصود!
سجل التدقيق: قد يُظهر انتقالاً مزدوجاً.

🟠 خطورة: متوسطة

ملخص: نقاط تضخيم الخطأ البشري
النقطة	الوصف	الخطورة
كاشف التكرار في الاستيراد	نص SQLite على PostgreSQL → لا يعمل أبداً	🔴
workflow-advance بدون transaction	انتقال جزئي ممكن	🔴
levenshtein على UTF-8	mb_levenshtein.php موجود لكن غير مستخدم	🟠
البحث لا يُطبِّع عربياً	نتائج "لا توجد" للبيانات الموجودة	🔴
normalizeAmount لا يُزيل SR	مبالغ بصيغة SR تُخزَّن كـ null	🔴
حذف مباشر يُفعِّل CASCADE	فقدان سجل قانوني بصمت	🔴
لا idempotency token	زر الرجوع قد يُقدِّم للمرحلة التالية	🟠
لا prompt "بيانات اختبار"	تلوث بيانات الإنتاج	🟠
Break Glass بلا حد يومي	يُصبح روتينياً ويُخفي الطوارئ الحقيقية	🟠
مناطق الاعتماد الزائد على السلوك الصحيح للمستخدم
صحة تنسيق المبلغ: النظام يفترض أرقاماً نقية — لا validation لمبالغ صغيرة جداً أو null.
اختيار "بيانات اختبار" صراحةً: لا آلية كشف تلقائي.
عدم استخدام واجهة الحذف المباشر: الحماية تعتمد على أن المستخدم يستخدم HistoryArchiveService.
انتظار المطابقة قبل تقديم للمراحل التالية: النظام يسمح بتقديم ضمان pending قبل اختيار الإجراء.
عدم الضغط المتكرر على الأزرار: لا حماية idempotency في workflow-advance.php.
