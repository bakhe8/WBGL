📘 تقرير WBGL التشغيلي الشامل (نسخة V3 المحدثة)

اسم النظام: WBGL
نوع الوثيقة: تقرير تشغيلي وصفي مع توصيف تدقيقي
تاريخ التحديث: 2026-02-18
نطاق التقرير: توصيف الحالة الفعلية للنظام بعد ترقيات شهر فبراير (Bilingual & Maint V2)

1) هوية النظام واستقلاله

النظام يعمل بهوية مستقلة تحت اسم WBGL. تم فصله بالكامل عن أي مشاريع سابقة وبربط مستودع مستقل.

1) الإعداد التشغيلي المحلي (Windows)

النظام مهيأ للعمل محلياً عبر:

- wbgl_server.ps1: سكربت الإدارة المركزي.
- toggle.bat: المشغل السريع.
- WBGL_8181.vbs: للتشغيل الصامت من الاختصار المكتبي.

1) نظام الدفعات (Batches) ودمج الموردين [جديد]

بالإضافة لنظام الدفعات القياسي، تم إضافة:

- **أداة دمج الموردين (Merge Tool)**: تتيح تنظيف البيانات عبر دمج موردين مكررين في مورد واحد مع الحفاظ على كافة السجلات المرتبطة.

1) قنوات الإدخال والمطابقة الثنائية [جديد]

- **الموردون ثنائيو اللغة**: يدعم النظام الآن الأسماء الإنجليزية (`english_name`) للموردين.
- **المطابقة الذكية المطورة**: محرك المطابقة يأخذ بالاعتبار الأسماء العربية والإنجليزية لرفع دقة التعرف التلقائي.

1) التتبع التاريخي وأداة الصيانة V2 [جديد]

- **Timeline & Snapshots**: العصب الأساسي للنظام، يسجل كل حدث مع نسخة كاملة قبل التغيير.
- **أداة الصيانة المتقدمة (`/maint/`)**: تسمح بتصحيح الأخطاء التقنية مع ميزة "تزامن التاريخ" (History Sync) التي تقوم بتحديث كافة السجلات التاريخية المرتبطة آلياً عند تعديل حقل معين.

1) الحالات والإجراءات

- الحالات: Needs Review -> Ready -> Released (Locked).
- الإجراءات: تمديد، تخفيض، إفراج.

1) الإنتاج (Production Mode)

عند التفعيل، يمنع إدخال بيانات الاختبار ويحمي بيانات الإنتاج الحقيقية في التقارير والطباعة.

1) المخرج الرسمي (الخطابات)

يولد النظام خطابات بنكية معيارية عبر LetterBuilder.php وقالب موحد لضمان اتساق المخرجات.

---
هذه الوثيقة تمثل المرجع الأساسي للحالة التشغيلية للنظام كما هي في 18 فبراير 2026.
