مفهوم تمامًا.
فيما يلي تقرير واحد شامل، مصحَّح، منقَّح، ومُوحَّد يعتمد على التقرير الذي زودتني به كما هو تشغيليًا، مع إدخال جميع التصحيحات والتوضيحات التي ثبتت خلال النقاش، وبصياغة تدقيقية محايدة وجاهزة للاعتماد.

🔒 مهم:
هذا التقرير:

لا يحتوي تناقضات لغوية أو مفاهيمية

يفرّق صراحة بين (الوصف / التقييم / المستقبل)

يعكس السلوك الحقيقي للنظام فقط

صالح كمرجع رسمي (Operational + Audit-Ready)

📘 تقرير WBGL التشغيلي الشامل (نسخة معتمدة)

اسم النظام: WBGL
نوع الوثيقة: تقرير تشغيلي وصفي مع توصيف تدقيقي
تاريخ التحديث: 2026-02-13
نطاق التقرير: توصيف الحالة الفعلية للنظام (As-Is)

1) هوية النظام واستقلاله

النظام يعمل بهوية مستقلة تحت اسم WBGL.

تم فصل النظام بالكامل عن أي مشروع سابق، بما يشمل:

المستودع البرمجي

مسارات التشغيل

الهوية النصية في الواجهات والوثائق

المستودع الرسمي:

git@github.com:bakhe8/WBGL.git

الفرع التشغيلي المعتمد: main

تم إنشاء وسم إصدار:

v1.0.0-wbgl

2) الإعداد التشغيلي المحلي (Windows)
الهدف

تشغيل WBGL محليًا على جهاز واحد دون تضارب مع مشاريع أخرى.

آلية التشغيل المعتمدة

سكربت إدارة مركزي:

wbgl_server.ps1

مشغّل سريع:

toggle.bat

تشغيل صامت للاختصار المكتبي:

WBGL_8181.vbs (محفوظ داخل جذر المشروع)

خصائص التشغيل

دعم أوامر: start / stop / restart / toggle

دعم منفذ ثابت عند الحاجة (مثل 8181)

منع تضارب المنافذ عبر إدارة تشغيل/إيقاف صريحة

ملف التشغيل الصامت ليس موجودًا على سطح المكتب بل يُستدعى منه فقط

3) سياسة الحفظ والإصدارات (Git)

يتم حفظ الحالة الكاملة للمشروع كما هي عند الطلب.

جميع التغييرات التنظيمية موثقة عبر commits واضحة.

الإصدار الحالي مرفوع ومُعتمد في GitHub.

4) تنظيم الملفات والوثائق

README محدث ويعكس الواقع التشغيلي الفعلي.

نقطة دخول واحدة لإدارة الخادم.

تنظيم واضح لمسارات التشغيل والخدمات.

5) نظام الدفعات (Batches)

النظام يحتوي على نظام دفعات تشغيلي فعلي.

الدفعة تمثل طبقة تنظيمية فوق السجلات الفردية.

تغطي دورة حياة الدفعة:

الإنشاء

المتابعة

عرض التفاصيل

الطباعة

الدفعات تُستخدم لأغراض:

المتابعة التشغيلية

التدقيق

التحليل الإحصائي

الملاحظات والمرفقات

يمكن إضافة ملاحظات لأي ضمان.

يمكن إرفاق مستندات داعمة.

الملاحظات والمرفقات لا تغيّر حالة الضمان، لكنها تُسجَّل كأحداث تاريخية.

6) قنوات الإدخال والاستيراد
القنوات المدعومة

استيراد Excel

إدخال يدوي

لصق ذكي (Smart Paste)

قواعد عامة

عمليات الإدخال الناجحة تسجّل أحداثها في Timeline.

جميع الأحداث تحفظ Snapshot كامل للكيان قبل التنفيذ.

المطابقة الذكية تُستدعى بعد الإدخال ولا تعيق نجاح عملية الإدخال نفسها.

التكرار

النظام يدعم إعادة محاولة إدخال نفس الضمان عبر:

Excel

اللصق الذكي

في حال التكرار:

لا يتم إنشاء سجل جديد

يتم تسجيل حدث استيراد مكرر

لا يتم تعديل الكيان أو حالته

ملاحظة:
في الإدخال اليدوي، تكرار رقم الضمان يُرفض قبل الإنشاء.

7) قواعد الرفض الصارم (Reject & Ignore)
حالات رفض الإدخال

تُرفض العملية دون إنشاء سجل جديد في حالات مثل:

نقص الحقول الإلزامية

طلب إنشاء بيانات اختبار أثناء تفعيل Production Mode

تكرار رقم الضمان في الإدخال اليدوي

في حالة عدم وجود تطابق بنكي في البيانات المرجعية:

لا يُرفض الإدخال تلقائيًا بسبب ذلك فقط

وقد يبقى الضمان بدون مطابقة بنك (pending) حتى تحديث البيانات المرجعية أو المعالجة اللاحقة

لا يتم:

إنشاء سجل فقط في حالات الرفض المذكورة أعلاه

أما في عدم التطابق البنكي فتُسجّل بيانات الضمان ويستمر المسار التشغيلي وفق حالة pending

8) Timeline والتتبع التاريخي
تعريف Timeline

Timeline يسجّل جميع الأحداث التشغيلية دون استثناء.

ليس مجرد سجل تقني، بل سجل تدقيقي تشغيلي.

Snapshot

قبل تنفيذ أي حدث:

يتم حفظ Snapshot كامل للكيان

يشمل جميع الحقول والعلاقات

عند استعراض أي حدث:

يمكن الرجوع للحالة الكاملة للضمان قبل الحدث

أنواع الأحداث المسجلة

الاستيراد

المطابقات (بنك / مورد)

انتقالات الحالة

الإجراءات التشغيلية

الملاحظات

المرفقات

محاولات الاستيراد المكرر

9) المطابقة (Bank vs Supplier)
مطابقة البنك

آلية فقط

غير متعلمة

تعتمد على البيانات المرجعية للبنوك

فشل المطابقة لا يوقف الإدخال تلقائيًا، ويؤدي لبقاء المطابقة البنكية غير مكتملة حتى المعالجة اللاحقة

مطابقة المورد

ذكية ومتعلّمة

تعتمد على:

Confirmations

Rejections

تعرض اقتراحات بدرجات ثقة

قرارات المستخدم تُسجَّل كأحداث مستقلة

10) الحالات والإجراءات
الحالات

Needs Review

Ready

Released (Locked)

الانتقالات

الانتقال من Needs Review → Ready:

يتم آليًا بعد اكتمال المتطلبات

ليس إجراءً يدويًا

الانتقال من Ready → Released:

يتم عبر إجراء إفراج

بعده يصبح الضمان Locked

الإجراءات التشغيلية الحالية (فقط)

تمديد

تخفيض

إفراج

أي إجراء آخر مذكور هو مستقبلي وغير منفذ حاليًا.

11) Production Mode

يمنع إنشاء بيانات اختبار جديدة.

لا يحذف بيانات الاختبار القديمة.

يستبعد بيانات الاختبار من:

العرض

التقارير

الطباعة

يعطّل أدوات إدارة بيانات الاختبار.

يوقف سجلات DEBUG فقط.

12) الإعدادات

تُدار من واجهة الإعدادات.

تُحفظ في:

storage/settings.json

تشمل:

عتبات المطابقة

أوزان التقييم

إعدادات التعلم

المنطقة الزمنية

Production Mode

13) الخطابات (المخرج الرسمي)

الخطاب البنكي هو المخرج الرسمي للنظام.

يتم توليده عبر:

LetterBuilder.php

قالب موحد ثابت

مبدأ Single Source of Truth يطبّق على الخطابات فقط.

14) صفحة الإحصائيات

تعتمد مباشرة على قاعدة البيانات.

تستبعد بيانات الاختبار في وضع الإنتاج.

تعرض مؤشرات:

تشغيلية

زمنية

تعلم

إجراءات

تحليل مالي

15) خاتمة

هذا التقرير:

يعكس الحالة التشغيلية الفعلية للنظام WBGL.

لا يحتوي تقييمات رقمية أو أحكام نوعية.

يفصل بين:

السلوك الحالي

التصميم المستقبلي

يصلح كمرجع:

تشغيلي

تدقيقي

توثيقي رسمي