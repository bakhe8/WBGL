# المرحلة الرابعة - مراجعة الأمن والموثوقية (Security & Reliability Review)

## التاريخ: 2026-02-11

---

## 📊 ملخص الحالة الأمنية

بما أن النظام مصمم كمشروع محلي لمستخدم واحد (Single User)، فإن العديد من ضوابط الأمان التقليدية (مثل تسجيل الدخول) غائبة طوعاً. ومع ذلك، هناك مخاطر تقنية يجب توثيقها في حال تم تعريض النظام لشبكة أو استخدامه في بيئة تشاركيه.

| التهديد | المستوى | الحالة |
|---------|---------|--------|
| **تسريب الأسرار (Secrets Leakage)** | 🟢 منخفض | لم يتم العثور على مفاتيح أو كلمات مرور |
| **حقن SQL (SQL Injection)** | 🟢 منخفض | استخدام ممتاز لـ Prepared Statements |
| **انتحال الطلبات (CSRF)** | 🔴 مرتفع | غياب كامل للحماية (لا يوجد CSRF Tokens) |
| **الصلاحيات (Auth/Authz)** | 🔴 مرتفع | لا يوجد أي نظام للتحقق من الهوية |
| **كشف الملفات (File Exposure)** | 🟡 متوسط | خادم `server.php` قد يسرب ملفات النظام |

---

## 🔍 النتائج التفصيلية (Security Findings)

### 1️⃣ الوصول غير المصرح به (Unauthorized Access)

- **المشكلة:** لا يوجد نظام `login`. أي شخص يصل لرابط الخادم (localhost:8089) يمكنه:
  - استيراد بيانات جديدة.
  - تعديل قرارات الضمانات.
  - حذف سجلات أو دفعات كاملة.
- **الأثر:** مخاطرة عالية بفقدان البيانات أو التلاعب بها إذا تم الوصول للجهاز عبر الشبكة المحلية.

### 2️⃣ ثغرات التعامل مع الملفات (File Handling)

- **المشكلة:** الثغرة في `server.php` (الراوتر) تسمح بالوصول المباشر للملفات.
- **التفاصيل:** الكود يمرر الطلب لخادم PHP المدمج إذا لم يكن الملف في قائمة الامتدادات المسموحة. هذا قد يسمح بتحميل قاعدة البيانات `storage/database/app.sqlite` مباشرة عبر المتصفح إذا تم تخمين المسار.
- **المخاطرة:** تسريب كامل لقاعدة البيانات التي تحتوي على معلومات مالية حساسة (قيمة الضمانات، أسماء الموردين، العقود).

### 3️⃣ غياب حماية CSRF

- **المشكلة:** الـ APIs (مثل `api/save-and-next.php`) تقبل طلبات POST بدون أي Tokens للتحقق من المصدر.
- **الأثر:** يمكن لموقع خبيث (إذا زاره المستخدم في متصفح آخر) إرسال طلبات خلف الكواليس لتعديل البيانات في لوحة WBGL المفتوحة.

### 4️⃣ الموثوقية (Reliability)

- **سجلات الأخطاء:** النظام يستخدم `storage/logs/app.log` بشكل جيد لتتبع العمليات، ولكن السجلات لا تشفر البيانات الحساسة (مثل أرقام العقود).
- **المدخلات:** فئة `App\Support\Input` تقوم بالتأكد من نوع البيانات (Type Checking) ولكنها لا تقوم بتنقية (Sanitization) متقدمة، مما يجعل النظام يعتمد كلياً على طبقة قاعدة البيانات للحماية.

---

## 📋 التوصيات (Recommendations)

1. **Network Binding:** التأكد من أن الخادم يعمل فقط على `127.0.0.1` وليس `0.0.0.0` لمنع الوصول من أجهزة أخرى في الشبكة.
2. **Database Access:** وضع ملف `.htaccess` أو منع الوصول المباشر لمجلد `storage` برمجياً في `server.php`.
3. **Basic Auth:** إضافة طبقة حماية بسيطة (Basic Authentication) حتى لو كان النظام لمستخدم واحد، لزيادة الموثوقية.

---

**✅ تمت الموافقة على الانتقال للمرحلة الخامسة والأخيرة (خطة الإصلاح الشاملة).**
