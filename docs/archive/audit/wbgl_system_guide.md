# دليل نظام WBGL الشامل — منطق العمل والأهداف والمستقبل

**التاريخ:** 2026-02-12 | **الإصدار:** v3.0

---

## 📌 ما هو WBGL؟

**WBGL** (Bank Guarantee Letters) هو نظام متخصص في **إدارة الضمانات البنكية** لمستشفى الملك فيصل التخصصي ومركز الأبحاث بالرياض. يحوّل عملية يدوية بطيئة ومعرضة للخطأ إلى تدفق رقمي ذكي شبه آلي.

---

## 🎯 المشكلة التي يحلها

### الوضع قبل النظام (يدوي)

المستشفى يتعامل مع مئات الموردين والمقاولين، وكل مورد يقدم **ضمان بنكي** عند توقيع عقد أو أمر شراء. هذه الضمانات:

| التحدي | الوصف |
|--------|-------|
| **تعدد المصادر** | ملفات Excel من إدارات مختلفة بصيغ غير موحدة |
| **أسماء غير موحدة** | نفس المورد يظهر بأسماء مختلفة (عربي/إنجليزي، اختصارات، أخطاء إملائية) |
| **مطابقة يدوية مرهقة** | كل اسم يحتاج مطابقة يدوية مع قاعدة البيانات الرسمية — بطيء وخطير |
| **خطابات رسمية** | كل ضمان يحتاج خطاب (إفراج/تمديد/تخفيض) بصيغة قانونية دقيقة |
| **ضياع التتبع** | صعوبة تتبع من فعل ماذا ومتى على كل ضمان |

### ماذا يفعل النظام؟

```
ملف Excel ← استيراد ← مطابقة ذكية ← قرار مستخدم ← خطاب رسمي ← طباعة
```

WBGL يأخذ ملفات Excel الخام، **يكتشف الأعمدة تلقائياً**، **يطابق أسماء الموردين والبنوك بذكاء**، **يتعلم من قرارات المستخدم**، وينتج **خطابات رسمية جاهزة للطباعة**.

---

## 🧠 منطق العمل (Business Logic)

### المفاهيم الأساسية

| المفهوم | الشرح |
|---------|-------|
| **الضمان البنكي** | وثيقة من بنك تضمن التزام المورد بالعقد. لها رقم ومبلغ وتاريخ صلاحية |
| **الدفعة (Batch)** | مجموعة ضمانات مستوردة معاً من ملف Excel واحد |
| **المورد (Supplier)** | الشركة المقدمة للضمان — لها اسم رسمي وأسماء بديلة |
| **البنك (Bank)** | الجهة المصدرة للضمان — لها اسم رسمي ومعلومات اتصال |
| **القرار (Decision)** | ربط الضمان بمورد وبنك محددين من قاعدة البيانات الرسمية |
| **الخطاب (Letter)** | وثيقة رسمية تُرسل للبنك لتنفيذ إجراء على الضمان |

### حالات الضمان

```
┌─────────┐     ربط مورد + بنك     ┌─────────┐     إصدار خطاب     ┌──────────┐
│ pending │ ──────────────────────→ │  ready  │ ────────────────→ │ released │
│ (معلق)  │                        │ (جاهز)  │                   │ (مُنفّذ) │
└─────────┘                        └─────────┘                   └──────────┘
    ↑                                   │
    └───────── إلغاء المورد/البنك ──────┘
```

- **pending**: لم يتم تحديد المورد أو البنك بعد
- **ready**: المورد والبنك محددان — جاهز لإصدار خطاب
- **released**: تم إصدار خطاب وقُفل الضمان

### الإجراءات الثلاثة

| الإجراء | الهدف | الشرط | الخطاب |
|---------|-------|-------|--------|
| **إفراج (Release)** | إعادة الضمان للبنك بعد انتهاء الغرض | جاهز + غير مقفل | يوقعه مساعد الرئيس التنفيذي |
| **تمديد (Extension)** | تمديد فترة صلاحية الضمان | جاهز + غير مقفل | يوقعه مدير العمليات المحاسبية |
| **تخفيض (Reduction)** | تخفيض مبلغ الضمان | جاهز + غير مقفل | يوقعه مدير العمليات المحاسبية |

---

## 🤖 نظام المطابقة الذكية

القلب الذكي للنظام. عند استيراد ضمان باسم مورد خام (مثل "شركة النورس المحدودة للمقاولات")، يبحث النظام في 5 مصادر:

```
                    ┌──────────────────────┐
                    │  الاسم الخام من Excel │
                    └──────────┬───────────┘
                               ↓
        ┌──────────────────────────────────────────┐
        │           5 مصادر إشارات (Signals)        │
        ├──────────────────────────────────────────┤
        │ 1. Alias     → أسماء بديلة معروفة (100%) │
        │ 2. Anchor    → استخراج كيانات عربية (90%) │
        │ 3. Fuzzy     → تشابه نصي (85-55%)        │
        │ 4. Learning  → قرارات سابقة مشابهة       │
        │ 5. Historical → تاريخ ربط هذا الاسم       │
        └──────────────────┬───────────────────────┘
                           ↓
        ┌──────────────────────────────────────────┐
        │    ConfidenceCalculatorV2                  │
        │    ← يجمع الإشارات + يحسب نسبة الثقة →    │
        │    ← قابل للتكوين عبر الإعدادات →         │
        └──────────────────┬───────────────────────┘
                           ↓
        ┌──────────────────────────────────────────┐
        │  النتيجة: "شركة النورس للمقاولات" — 89%  │
        │           "النورس التجارية" — 45%         │
        │           "النورس للصناعة" — 31%          │
        └──────────────────────────────────────────┘
```

**كيف يتعلم؟** عندما يوافق المستخدم على اقتراح أو يختار مورداً مختلفاً، يسجل النظام ذلك. المرة التالية التي يرى فيها نفس الاسم → ثقة أعلى.

---

## 🔄 خطوات العمل الكاملة

### الخطوة 1: استيراد ملف Excel

1. المستخدم يرفع ملف `.xlsx` عبر الواجهة
2. النظام يكتشف أعمدة الملف تلقائياً (يبحث في أول 5 صفوف)
3. لكل صف:
   - يستخرج البيانات (رقم الضمان، المورد، البنك، المبلغ، التواريخ، العقد)
   - يتحقق من التكرار (duplicate check)
   - يحفظ البيانات الخام كـ JSON
4. يُسجل الدفعة في نظام الدفعات

### الخطوة 2: المطابقة التلقائية

1. بعد الاستيراد مباشرة، يعمل `SmartProcessingService` تلقائياً
2. لكل ضمان مستورد:
   - يُطبّع اسم البنك → يحاول المطابقة التلقائية
   - يُطبّع اسم المورد → يجمع الإشارات من 5 مصادر
   - إذا الثقة ≥ 95% → قبول تلقائي (Auto-Accept)
3. النتائج: عدد المستورد + المطابق تلقائياً

### الخطوة 3: مراجعة واتخاذ القرارات

1. المستخدم يفتح الواجهة الرئيسية
2. يرى الضمان + اقتراحات الموردين مرتبة بنسبة الثقة
3. يختار المورد المطابق (أو يُنشئ جديداً)
4. يحفظ → النظام:
   - يُحدّث الحالة (pending ← ready)
   - يُسجل التعلم (confirm + reject ضمني)
   - يُسجل حدث في التاريخ الزمني
   - ينتقل تلقائياً للضمان التالي

### الخطوة 4: إصدار الخطابات

1. المستخدم ينقر "إفراج" أو "تمديد" أو "تخفيض"
2. النظام يتحقق من الشروط (جاهز + غير مقفل)
3. `LetterBuilder` يبني الخطاب:
   - ترويسة (اسم البنك + العنوان)
   - موضوع (نوع الإجراء + رقم الضمان)
   - محتوى (فقرات بصيغة قانونية)
   - توقيع (يختلف حسب نوع الإجراء)
4. الخطاب يُعرض كمعاينة HTML
5. المستخدم يطبع (Ctrl+P → PDF)

### الخطوة 5: المعالجة الجماعية (اختياري)

- يمكن تمديد أو تخفيض دفعة كاملة مرة واحدة
- تحديد الضمانات + إدخال القيمة الجديدة → تنفيذ جماعي

---

## ⚙️ المتطلبات التقنية

### متطلبات التشغيل

| المكون | الحد الأدنى | الموصى به |
|--------|-------------|-----------|
| **PHP** | 8.0 | 8.3+ |
| **SQLite3** | مبني مع PHP | — |
| **Composer** | اختياري (للاعتماديات) | 2.x |
| **نظام التشغيل** | Windows / Linux / macOS | Windows |
| **المتصفح** | أي متصفح حديث | Chrome / Edge |

### الاعتماديات

| الحزمة | الإصدار | الغرض |
|--------|---------|-------|
| `phpoffice/phpspreadsheet` | ^1.29 | قراءة ملفات Excel |
| `phpunit/phpunit` | ^12.5 | اختبارات (تطوير فقط) |

### التشغيل

```powershell
# الطريقة 1: مباشرة
php -S localhost:8089 server.php

# الطريقة 2: سكربت PowerShell
.\toggle.ps1

# الطريقة 3: ملف Batch
.\toggle.bat
```

قاعدة البيانات `storage/database/app.sqlite` تُنشأ تلقائياً عند أول تشغيل.

---

## 📊 البنية التقنية

```
WBGL/
├── index.php              ← نقطة الدخول الرئيسية (1058 سطر)
├── server.php             ← موجه السيرفر المدمج
│
├── app/                   ← قلب التطبيق
│   ├── Models/ (9)        ← كيانات البيانات
│   ├── Repositories/ (13) ← طبقة الوصول لقاعدة البيانات
│   ├── Services/ (38)     ← منطق العمل + المطابقة الذكية
│   ├── Support/ (15)      ← أدوات مساعدة (Database, Config, Normalizers)
│   ├── Contracts/ (1)     ← واجهات برمجية (Interfaces)
│   └── DTO/ (2)           ← كائنات نقل البيانات
│
├── api/ (36)              ← نقاط API (REST-like)
├── views/ (8)             ← صفحات كاملة
├── partials/ (12)         ← مكونات واجهة قابلة لإعادة الاستخدام
│
├── public/
│   ├── css/ (6)           ← نظام تصميم مخصص
│   └── js/ (8)            ← JavaScript (Vanilla)
│
├── storage/
│   ├── database/          ← قاعدة بيانات SQLite
│   ├── logs/              ← سجلات التطبيق
│   └── attachments/       ← مرفقات الضمانات
│
└── templates/             ← قوالب الخطابات الرسمية
```

### الأنماط المعمارية المستخدمة

| النمط | الموقع | الوصف |
|-------|--------|-------|
| **Repository Pattern** | `app/Repositories/` | فصل كامل بين SQL ومنطق العمل |
| **Service Layer** | `app/Services/` | منطق العمل في خدمات مستقلة |
| **Signal Pattern** | `Services/Learning/Feeders/` | 5 مصادر إشارات مستقلة للمطابقة |
| **Strategy Pattern** | `LetterBuilder` | محتوى الخطاب يتغير حسب نوع الإجراء |
| **Singleton** | `Database.php` | اتصال واحد بقاعدة البيانات |

---

## 🔐 الأمان والقيود الحالية

| الجانب | الحالة | ملاحظة |
|--------|--------|--------|
| **مصادقة المستخدمين** | ❌ غير موجودة | النظام مصمم لمستخدم واحد محلياً |
| **CSRF Protection** | ❌ غير موجودة | لا توجد حماية ضد الطلبات العابرة |
| **SQL Injection** | ✅ محمي | Prepared Statements في كل الاستعلامات |
| **XSS** | ✅ جزئي | `htmlspecialchars` في الـ partials |
| **حماية الملفات** | ⚠️ ضعيفة | `storage/` غير محمي من الوصول المباشر |
| **تشفير** | ❌ غير موجود | البيانات مخزنة كنص عادي |

---

## 🔮 تصور المستقبل

### تحسينات قصيرة المدى (1-3 أشهر)

1. **تأمين الوصول**: إضافة Basic Auth + منع الوصول لمجلد storage
2. **تفكيك index.php**: تقسيم الملف إلى Controller + Services
3. **إضافة Unit Tests**: تغطية أساسية للخدمات الجوهرية
4. **إضافة CSRF Tokens**: حماية عمليات POST

### تحسينات متوسطة المدى (3-6 أشهر)

1. **نظام صلاحيات**: Login + أدوار (مدير / مراجع / مدخل بيانات)
2. **Migration System**: تتبع تطور قاعدة البيانات
3. **واجهة API موثقة**: توثيق كامل لجميع الـ endpoints
4. **تحسين التعلم**: إضافة آلية مراجعة ثنائية + انتهاء صلاحية البيانات القديمة

### تطلعات بعيدة المدى (6-12 شهر)

1. **دعم متعدد المستخدمين**: وضع متعدد المستخدمين عبر الشبكة
2. **لوحة تقارير متقدمة**: تقارير مالية وإدارية تفاعلية
3. **تكامل خارجي**: ربط مع أنظمة ERP أو SAP
4. **تطبيق جوال**: عرض حالة الضمانات عبر الهاتف
5. **ذكاء اصطناعي حقيقي**: نماذج NLP لاستخراج الكيانات بدلاً من Heuristics

---

## 📋 ملخص الفوائد

| لمن؟ | الفائدة |
|------|---------|
| **موظف الضمانات** | توفير 80%+ من وقت المطابقة اليدوية |
| **الإدارة المالية** | خطابات رسمية دقيقة وجاهزة للطباعة فوراً |
| **المراجعة الداخلية** | سجل كامل لكل حدث على كل ضمان (Audit Trail) |
| **المؤسسة** | تقليل المخاطر المالية من أخطاء المطابقة |

---

**النظام مبني بـ 🤍 في المملكة العربية السعودية**
